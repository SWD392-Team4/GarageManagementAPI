namespace GarageManagementAPI.Shared.Enums
{
    public enum ServiceAction
    {
        Inspect = 1, // Tra (Inspect/Check)
        Replace = 2, // Thay thế (Replace)
        Lubricate = 3, // Bôi trơn (Lubricate)
        Align = 4, // Căn chỉnh (Align)
        Refill = 5, // Nạp (Refill/Recharge)
        Repair = 6, // Sửa chữa (Repair)
        Clean = 7, // Làm sạch (Clean)
        Upgrade = 8, // Nâng cấp (Upgrade)
        Restore = 9, // Phục hồi (Restore)
        Update = 10, // Cập nhật phần mềm (Update)
        Polish = 11, // Đánh bóng (Polish)
        Protect = 12, // Bảo vệ (Protect)
        Deodorize = 13, // Khử mùi (Deodorize)
        Condition = 14, // Dưỡng (Condition)
        Remove = 15, // Loại bỏ (Remove)
        RestoreLighting = 16 // Phục hồi ánh sáng (Restore Lighting)
    }
}
